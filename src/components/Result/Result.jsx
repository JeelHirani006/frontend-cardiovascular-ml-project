import RiskScore from './RiskScore/RiskScore';
import InputSummary from './InputSummary/InputSummary';
import BeatingHeart from './BeatingHeart/BeatingHeart';
import SignalVisualization from '../SignalVisualization/SignalVisualization';
import styles from './Result.module.css';

// Color matched to prediction outcome
const RESULT_COLORS = {
  0: '#1a9e6e',  // teal-green — no risk
  1: '#c0002a',  // crimson    — risk detected
};

export default function Result({ result, formData, onRestart }) {
  const { prediction } = result;
  const ecgColor = RESULT_COLORS[prediction] ?? RESULT_COLORS[0];

  return (
    <section className={styles.result} aria-label="Assessment result">

      {/* Full-page scrolling ECG background */}
      <div className={styles.bgCanvas} aria-hidden="true">
        <SignalVisualization variant="result" color={ecgColor} />
        {/* Radial vignette so content stays readable */}
        <div className={styles.bgFade} />
      </div>

      <div className={styles.inner}>
        <div className={styles.header}>
          <div className={styles.headerMeta}>
            <span className={styles.eyebrow}>
              <span className={styles.eyebrowLine} />
              CARDIOVASCULAR ASSESSMENT
            </span>
          </div>
          <h1 className={styles.title}>Your risk profile</h1>
          <p className={styles.subtitle}>
            Based on the health indicators you provided, the classification model has generated the following result.
          </p>
        </div>

        <div className={styles.scoreSection}>
          <BeatingHeart prediction={prediction} />
          <RiskScore prediction={prediction} />
        </div>

        <div className={styles.summarySection}>
          <InputSummary formData={formData} />
        </div>

        <div className={styles.disclaimer} role="note">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" aria-hidden="true">
            <circle cx="12" cy="12" r="10"/>
            <line x1="12" y1="8" x2="12" y2="12"/>
            <line x1="12" y1="16" x2="12.01" y2="16"/>
          </svg>
          <p>
            This assessment is generated by a machine-learning classification model and is intended for
            informational purposes only. It does not constitute a medical diagnosis or replace
            professional medical advice. Consult a qualified healthcare provider for any
            health concerns.
          </p>
        </div>

        <div className={styles.actions}>
          <button className={styles.restartBtn} onClick={onRestart}>
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
              <polyline points="1 4 1 10 7 10"/>
              <path d="M3.51 15a9 9 0 1 0 .49-3.11"/>
            </svg>
            Begin new assessment
          </button>
        </div>
      </div>
    </section>
  );
}
